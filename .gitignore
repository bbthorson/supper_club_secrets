# macOS
.DS_Store

# Windows
Thumbs.db

# Temp files
*.tmp
*~

# Compiled book build artifacts
*.epub

# Claude Code local machine config (permission allowlists etc.)
.claude/settings.local.json

# Python bytecode cache. Was pinned to protocol/entities and protocol/pipeline,
# both of which went away when the pipeline moved to the pinakes package and
# records/ replaced protocol/records/ — so tools/ was left uncovered.
__pycache__/

# Reader-experience site (Astro, Protocol Phase 3) build artifacts
site/node_modules/
site/dist/
site/.astro/

# Prose-check reports (regenerate with tools/prose_check.py)
.prose/
